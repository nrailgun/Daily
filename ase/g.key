\sorts {
	M;
}

\functions{
	M m(M,M);
	M e;
}

\problem {

	(
		\forall M x; m(e, x) = x
		&
		\forall M x; \forall M y; m(x, y) = m(y, x)
	) -> (
		\forall M e1; \forall M e2; (
			\forall M x; (m(e1, x) = x & m(e2, x) = x) -> e1 = e2)
	)
}
